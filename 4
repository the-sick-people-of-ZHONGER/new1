cltype clfindnode(cltype head,String key)
{
  cltype htemp;
  htemp=head;
  while(htemp=null)
  {
    if(htemp.nodedata.key.compareto(key)==0
    {
      return htemp;
    }
    htemp=htemp.nextnode;
  }
  return null;
}
